# MATRIX Reference Code

This area is for read-only implementation research.

Clone external repositories under `repos/` only when they are useful as references. Do not run production workflows from this directory, and do not copy code into AntiQuotar without checking license and attribution requirements.

Operational upstream clones used by `AntiQuotar.bat` belong in `tools/`, not here.
